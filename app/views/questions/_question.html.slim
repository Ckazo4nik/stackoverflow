#question_title = question.title
#question_body = question.body
#link_show = link_to 'Show', question
#link_edit = link_to 'Edit question', edit_question_path(question)
#link_destroy_answer = link_to 'Destroy', question, method: :delete, data: { confirm: 'Are you sure?' }
#question_user = question.user.email

